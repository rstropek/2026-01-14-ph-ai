Super, Danke! Bitte verwende den Playwright MCP Server, um in der Anwendung folgendes zu machen:

- Erstelle eine Klasse (verwende sinnvolle Testdaten)
- Erstelle zwei Schüler:innen in dieser Klasse (verwende sinnvolle Testdaten)
- Probiere die Filterfunktionen aus
- Nach jeder Änderungsoperation in der UI, verwende das `sqlite3` Kommandozeilen-Tool, um die Datenbank zu inspizieren und sicherzustellen, dass die Daten korrekt gespeichert wurden.
